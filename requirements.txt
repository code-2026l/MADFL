# ============================================================
# Pinned environment used to produce the numbers reported in the
# WSDM'27 paper (28-fold walk-forward, CSI 500). Version drift in
# xgboost/lightgbm/numpy can shift per-fold IC by ~1e-3, so these
# exact versions are required for bit-level reproduction; aggregate
# conclusions are robust to a minor bump, but please pin for
# verification of the reported tables.
# ============================================================

numpy==1.26.4
pandas==2.1.4
scipy==1.16.3
scikit-learn==1.7.2
xgboost==2.0.3
lightgbm==3.3.5
numba>=0.58
pyyaml==6.0.3
matplotlib>=3.7

# Deep learning (optional: LSTM baselines, rotating-MNIST adaptation)
torch>=2.0

# HMM regime detection
hmmlearn>=0.3

# Optional: LLM debate client (used only when an API key is configured).
# The framework ships a deterministic rule-based fallback that runs without it.
openai>=1.0
